# Техническое задание: Instagram Content Factory

## 1. Цель проекта

Разработать веб-сервис для автоматизированного постинга контента на множество Instagram-аккаунтов с автоматическим переводом текста под каждую аудиторию.

## 2. Функциональные требования

### 2.1 Управление аккаунтами Instagram

**Возможности:**
- Добавление аккаунтов (username, password)
- Авторизация и сохранение сессии
- Группировка аккаунтов по категориям (IT, Финансы, Новости, etc.)
- Привязка языка к каждому аккаунту
- Привязка прокси к каждому аккаунту (1 аккаунт = 1 прокси)
- Отображение статуса аккаунта:
  - `active` — готов к постингу
  - `banned` — получил бан от Instagram
  - `cooldown` — в режиме охлаждения (нельзя постить временно)
  - `login_required` — требуется повторная авторизация
  - `proxy_error` — проблемы с прокси

**Поля БД (таблица `accounts`):**
```sql
id: UUID
username: String (уникальный)
password: String (encrypted)
session_data: JSON (сессия instagrapi)
group_id: UUID (FK → groups)
language: String (en, ru, es, etc.)
proxy_url: String (http://user:pass@ip:port)
proxy_type: String (http, socks5)
status: Enum
last_post_at: Timestamp
last_login_at: Timestamp
posts_count_today: Integer
failed_attempts: Integer
created_at: Timestamp
updated_at: Timestamp
```

### 2.2 Управление группами

**Возможности:**
- Создание групп (IT, Финансы, Новости)
- Привязка аккаунтов к группам
- Просмотр всех аккаунтов в группе

**Поля БД (таблица `groups`):**
```sql
id: UUID
name: String (уникальный)
description: Text
accounts_count: Integer
created_at: Timestamp
```

### 2.3 Создание и публикация постов

**Возможности:**
- Загрузка медиа (фото, видео, карусель до 10 медиа)
- Ввод текста (caption) на исходном языке
- Выбор групп для публикации
- Автоматический перевод текста для каждого языка
- Предпросмотр переведённых текстов перед публикацией
- Ручное редактирование переводов (если нужно)
- Публикация:
  - Немедленно
  - Запланированная (scheduled)
- Отслеживание статуса публикации по каждому аккаунту

**Поля БД (таблица `posts`):**
```sql
id: UUID
media_paths: JSON (массив путей к медиа)
media_type: Enum (photo, video, carousel, reel)
caption_original: Text
original_language: String
target_groups: JSON (массив group_id)
status: Enum (draft, pending, posting, completed, failed)
scheduled_at: Timestamp (nullable)
created_at: Timestamp
posted_at: Timestamp (nullable)
```

**Поля БД (таблица `post_executions`):**
```sql
id: UUID
post_id: UUID (FK → posts)
account_id: UUID (FK → accounts)
caption_translated: Text
instagram_media_id: String (ID медиа в Instagram)
status: Enum (queued, posting, success, failed)
error_message: Text (nullable)
retry_count: Integer
posted_at: Timestamp (nullable)
created_at: Timestamp
```

### 2.4 Система переводов

**Требования:**
- Интеграция с DeepL API (предпочтительно) или Google Translate API
- Кэширование переводов (если тот же текст + язык → использовать кэш)
- Сохранение оригинала и всех переводов в БД
- Возможность ручной корректировки перевода

**Поля БД (таблица `translations_cache`):**
```sql
id: UUID
text_original: Text
language_from: String
language_to: String
text_translated: Text
service: String (deepl, google)
created_at: Timestamp
```

**Логика перевода:**
1. Проверить кэш (text + language_from + language_to)
2. Если есть → вернуть из кэша
3. Если нет → вызвать API → сохранить в кэш → вернуть

## 3. Нефункциональные требования

### 3.1 Система проксирования

**Требования:**
- Каждый аккаунт работает через свой прокси
- Типы прокси: HTTP, SOCKS5
- Residential прокси (не датацентр!) для минимизации банов
- Ротация прокси при ошибках
- Проверка работоспособности прокси перед использованием

**Поля БД (таблица `proxies`):**
```sql
id: UUID
url: String (http://user:pass@ip:port)
type: Enum (http, socks5)
country: String
status: Enum (active, failed, checking)
last_check_at: Timestamp
success_rate: Float (0.0 - 1.0)
assigned_accounts: JSON (массив account_id)
created_at: Timestamp
```

**Логика работы с прокси:**
1. При добавлении аккаунта → назначить свободный прокси
2. Перед каждым действием → проверить статус прокси
3. Если прокси failed → попытаться переключить на резервный
4. Если прокси недоступен → пометить аккаунт как `proxy_error`

### 3.2 Защита от банов Instagram

**Механизмы:**
- **Задержки между действиями:**
  - Между постами с одного аккаунта: минимум 30 мин
  - Между постами на разные аккаунты: random 2-5 мин
  - При ошибках: экспоненциальная задержка (1 мин → 5 мин → 15 мин → 1 час)

- **Лимиты действий:**
  - Максимум 10 постов в день с одного аккаунта
  - Максимум 50 одновременных задач постинга
  - При достижении лимита → `cooldown` на 24 часа

- **Эмуляция поведения:**
  - User-Agent мобильного приложения Instagram
  - Разные устройства для разных аккаунтов (device_id в сессии)
  - Сохранение cookies и сессий между запусками

- **Прогрев аккаунтов:**
  - Новые аккаунты: первые 3 дня — 1-2 поста в день
  - После 3 дней: постепенное увеличение до 10 постов/день
  - Отслеживание `account_age` и корректировка лимитов

**Поля БД (добавить в `accounts`):**
```sql
device_id: String (уникальный ID устройства)
user_agent: String
posts_limit_per_day: Integer (по умолчанию 10)
account_age_days: Integer (вычисляемое поле)
warmup_stage: Integer (0-3, где 3 = полный прогрев)
```

### 3.3 Очередь задач (Celery)

**Архитектура:**
- Redis как брокер сообщений
- Несколько воркеров для параллельной обработки
- Приоритеты задач:
  - `high` — срочные посты
  - `normal` — обычные посты
  - `low` — повторные попытки после ошибок

**Типы задач:**
1. `task_post_to_instagram` — публикация на один аккаунт
2. `task_batch_post` — массовая публикация (создаёт задачи для каждого аккаунта)
3. `task_check_account_status` — проверка статуса аккаунта
4. `task_check_proxy` — проверка работы прокси
5. `task_translate_text` — перевод текста
6. `task_cleanup_old_sessions` — очистка устаревших сессий

**Параметры Celery:**
```python
CELERY_BROKER_URL = 'redis://localhost:6379/0'
CELERY_RESULT_BACKEND = 'redis://localhost:6379/0'
CELERY_TASK_SERIALIZER = 'json'
CELERY_RESULT_SERIALIZER = 'json'
CELERY_TIMEZONE = 'UTC'
CELERY_TASK_TRACK_STARTED = True
CELERY_TASK_TIME_LIMIT = 300  # 5 минут на задачу
CELERY_TASK_SOFT_TIME_LIMIT = 240  # мягкий лимит 4 минуты
```

### 3.4 Безопасность

**Шифрование:**
- Пароли аккаунтов: bcrypt или argon2
- Прокси credentials: Fernet (симметричное шифрование)
- Переменные окружения для API ключей

**Аутентификация в админке:**
- JWT токены
- Роли: `admin` (полный доступ), `editor` (только постинг)

**Поля БД (таблица `users`):**
```sql
id: UUID
username: String (уникальный)
email: String (уникальный)
password_hash: String
role: Enum (admin, editor)
is_active: Boolean
last_login_at: Timestamp
created_at: Timestamp
```

### 3.5 Мониторинг и логирование

**Метрики:**
- Успешность постинга (success rate) по каждому аккаунту
- Количество активных/забаненных аккаунтов
- Среднее время публикации
- Статус очереди Celery (ожидающие/выполняемые задачи)

**Логи:**
- Все действия с Instagram API (запрос → ответ)
- Ошибки с полным traceback
- История изменений аккаунтов (status change)

**Поля БД (таблица `activity_logs`):**
```sql
id: UUID
account_id: UUID (FK, nullable)
action: String (login, post, check_status, etc.)
status: Enum (success, failed)
details: JSON (запрос/ответ)
error_message: Text (nullable)
duration_ms: Integer
created_at: Timestamp
```

**Dashboard метрик:**
- График постов по времени
- Статистика по группам
- Топ-5 проблемных аккаунтов
- Статус очереди в реальном времени

## 4. Технический стек

### 4.1 Backend
- **Python 3.11+**
- **FastAPI** — веб-фреймворк
- **SQLAlchemy 2.0** — ORM
- **Alembic** — миграции БД
- **Celery** — очередь задач
- **Redis** — брокер для Celery + кэш
- **PostgreSQL** — основная БД
- **instagrapi** — работа с Instagram
- **DeepL API** — переводы
- **cryptography** — шифрование
- **pydantic** — валидация данных

### 4.2 Frontend (админка)
- **Jinja2 templates + HTMX** (быстрая реализация) ИЛИ
- **React + Vite** (если нужен современный UI)

### 4.3 Инфраструктура
- **Docker + Docker Compose** — контейнеризация
- **Nginx** — reverse proxy
- **Supervisor** — управление Celery workers
- **Sentry** (опционально) — мониторинг ошибок

## 5. Структура проекта

```
instagram_cf/
├── backend/                     # Весь backend код
│   ├── alembic/                # Миграции БД
│   │   ├── versions/
│   │   ├── env.py
│   │   └── script.py.mako
│   ├── alembic.ini
│   ├── app/
│   │   ├── api/                 # FastAPI endpoints
│   │   │   ├── accounts.py
│   │   │   ├── groups.py
│   │   │   ├── posts.py
│   │   │   └── proxies.py
│   │   ├── core/                # Конфигурация
│   │   │   ├── config.py
│   │   │   ├── security.py
│   │   │   └── database.py
│   │   ├── models/              # SQLAlchemy модели
│   │   │   ├── account.py
│   │   │   ├── group.py
│   │   │   ├── post.py
│   │   │   ├── proxy.py
│   │   │   └── user.py
│   │   ├── schemas/             # Pydantic схемы
│   │   │   ├── account.py
│   │   │   ├── group.py
│   │   │   ├── post.py
│   │   │   └── proxy.py
│   │   ├── services/            # Бизнес-логика
│   │   │   ├── instagram.py     # Работа с instagrapi
│   │   │   ├── translator.py    # Переводы
│   │   │   ├── proxy_manager.py # Управление прокси
│   │   │   └── post_scheduler.py # Планирование постов
│   │   ├── tasks/               # Celery задачи
│   │   │   ├── posting.py
│   │   │   ├── translation.py
│   │   │   └── monitoring.py
│   │   ├── utils/               # Утилиты
│   │   │   ├── logging.py
│   │   │   └── validators.py
│   │   └── main.py              # Точка входа FastAPI
│   ├── celery_app/              # Celery конфигурация
│   │   ├── worker.py
│   │   └── config.py
│   └── static/                  # Загруженные медиа
│       └── uploads/
├── tests/                       # Тесты
│   ├── test_instagram.py
│   ├── test_translator.py
│   └── test_api.py
├── .env                         # Переменные окружения
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
├── README.md
├── TZ.md                        # Это ТЗ
├── changelog.md                 # История изменений
└── context_memory.md            # Лог разговоров
```

## 6. API Endpoints (спецификация)

### 6.1 Аккаунты

```
POST   /api/accounts/               - Добавить аккаунт
GET    /api/accounts/               - Список всех аккаунтов
GET    /api/accounts/{id}           - Информация об аккаунте
PUT    /api/accounts/{id}           - Обновить аккаунт
DELETE /api/accounts/{id}           - Удалить аккаунт
POST   /api/accounts/{id}/login     - Авторизоваться в Instagram
GET    /api/accounts/{id}/status    - Проверить статус аккаунта
```

### 6.2 Группы

```
POST   /api/groups/                 - Создать группу
GET    /api/groups/                 - Список групп
GET    /api/groups/{id}             - Информация о группе
PUT    /api/groups/{id}             - Обновить группу
DELETE /api/groups/{id}             - Удалить группу
GET    /api/groups/{id}/accounts    - Аккаунты в группе
```

### 6.3 Посты

```
POST   /api/posts/                  - Создать пост (загрузить медиа + текст)
GET    /api/posts/                  - Список постов
GET    /api/posts/{id}              - Информация о посте
PUT    /api/posts/{id}              - Обновить пост
DELETE /api/posts/{id}              - Удалить пост
POST   /api/posts/{id}/publish      - Запустить публикацию
GET    /api/posts/{id}/executions   - Статус публикации по аккаунтам
POST   /api/posts/{id}/translate    - Получить переводы для превью
```

### 6.4 Прокси

```
POST   /api/proxies/                - Добавить прокси
GET    /api/proxies/                - Список прокси
GET    /api/proxies/{id}            - Информация о прокси
PUT    /api/proxies/{id}            - Обновить прокси
DELETE /api/proxies/{id}            - Удалить прокси
POST   /api/proxies/{id}/check      - Проверить работу прокси
```

### 6.5 Мониторинг

```
GET    /api/stats/overview          - Общая статистика
GET    /api/stats/accounts          - Статистика по аккаунтам
GET    /api/stats/posts             - Статистика по постам
GET    /api/logs/                   - Логи системы (пагинация)
```

## 7. Пример работы системы

### Сценарий: публикация поста на 20 аккаунтов в группе "IT"

1. **Админ загружает пост:**
   - Медиа: `image.jpg`
   - Текст: "Новый курс по Python! Скидка 50% 🔥"
   - Группы: ["IT"]

2. **Система создаёт запись в БД:**
   - `posts.status = 'draft'`
   - Сохраняет медиа в `static/uploads/`

3. **Админ нажимает "Опубликовать":**
   - API: `POST /api/posts/{id}/publish`
   - Получает список аккаунтов в группе "IT" (20 штук)
   - Определяет языки: 10 × en, 5 × ru, 5 × es

4. **Система переводит текст:**
   - en: "New Python course! 50% off 🔥"
   - ru: "Новый курс по Python! Скидка 50% 🔥" (оригинал)
   - es: "¡Nuevo curso de Python! 50% de descuento 🔥"
   - Сохраняет в `translations_cache`

5. **Celery создаёт задачи:**
   - 20 задач `task_post_to_instagram`
   - Каждая задача с задержкой: random(120, 300) секунд
   - Добавляет в очередь с приоритетом `normal`

6. **Workers выполняют задачи:**
   - Берут задачу из очереди
   - Загружают аккаунт + сессию + прокси
   - Подключаются к Instagram через прокси
   - Постят медиа с переведённым текстом
   - Сохраняют результат в `post_executions`
   - Обновляют `accounts.last_post_at`

7. **Мониторинг:**
   - Админ видит в реальном времени: 15/20 опубликовано, 3/20 в очереди, 2/20 ошибка
   - При ошибках: автоматический retry через 5 минут (максимум 3 попытки)

8. **Завершение:**
   - Все 20 задач выполнены
   - `posts.status = 'completed'`
   - Уведомление админу (опционально)

## 8. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Массовые баны аккаунтов | Высокая | Критическое | Прокси, задержки, лимиты, прогрев аккаунтов |
| Падение прокси | Средняя | Высокое | Пул резервных прокси, автоматическая ротация |
| Изменения API Instagram | Средняя | Высокое | Использование instagrapi (активно поддерживается) |
| Низкое качество переводов | Средняя | Среднее | DeepL (лучше Google), ручная корректировка |
| Нагрузка на БД | Низкая | Среднее | Индексы, connection pooling, Redis кэш |

## 9. План разработки (этапы)

### Этап 1: Основа (3-5 дней)
- [ ] Настроить проект (FastAPI + PostgreSQL + Docker)
- [ ] Создать модели БД и миграции
- [ ] Реализовать CRUD для аккаунтов и групп
- [ ] Базовая админка (список аккаунтов, групп)

### Этап 2: Instagram интеграция (3-4 дня)
- [ ] Интегрировать instagrapi
- [ ] Реализовать авторизацию и сохранение сессий
- [ ] Тестовый постинг на 1 аккаунт
- [ ] Обработка ошибок Instagram

### Этап 3: Проксирование (2-3 дня)
- [ ] CRUD для прокси
- [ ] Привязка прокси к аккаунтам
- [ ] Проверка работоспособности прокси
- [ ] Автоматическая ротация при ошибках

### Этап 4: Переводы (1-2 дня)
- [ ] Интеграция DeepL/Google Translate API
- [ ] Кэширование переводов
- [ ] API для получения переводов

### Этап 5: Система постинга (4-5 дней)
- [ ] Загрузка медиа (фото, видео)
- [ ] Создание постов
- [ ] Celery задачи для массового постинга
- [ ] Очередь с задержками и приоритетами
- [ ] Отслеживание статуса публикации

### Этап 6: Защита от банов (2-3 дня)
- [ ] Лимиты действий
- [ ] Система прогрева аккаунтов
- [ ] Экспоненциальные задержки при ошибках
- [ ] Автоматический cooldown

### Этап 7: Мониторинг (2-3 дня)
- [ ] Логирование всех действий
- [ ] Dashboard метрик
- [ ] Алерты при критических ошибках

### Этап 8: Финализация (2-3 дня)
- [ ] Тестирование на реальных аккаунтах
- [ ] Документация API
- [ ] Инструкция по развёртыванию
- [ ] Оптимизация производительности

**Общее время: 19-28 дней**

## 10. Переменные окружения (.env)

```bash
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/instagram_cf

# Redis
REDIS_URL=redis://localhost:6379/0

# Instagram
INSTAGRAM_SESSION_LIFETIME_DAYS=90

# Translation
DEEPL_API_KEY=your_deepl_api_key
# or
GOOGLE_TRANSLATE_API_KEY=your_google_api_key

# Security
SECRET_KEY=your_secret_key_for_jwt
ENCRYPTION_KEY=your_fernet_key_for_passwords

# Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Limits
MAX_POSTS_PER_DAY_PER_ACCOUNT=10
MAX_CONCURRENT_TASKS=50
MIN_DELAY_BETWEEN_POSTS_SEC=120
MAX_DELAY_BETWEEN_POSTS_SEC=300

# Monitoring
SENTRY_DSN=your_sentry_dsn  # optional
```

## 11. Критерии приёмки

- [ ] Можно добавить 50+ аккаунтов с прокси
- [ ] Можно создать группы и распределить аккаунты
- [ ] Загрузка медиа (фото, видео) работает
- [ ] Автоматический перевод на 5+ языков
- [ ] Публикация на 20+ аккаунтов одновременно без банов
- [ ] Задержки между постами работают корректно
- [ ] При ошибках система автоматически делает retry
- [ ] Dashboard показывает статус всех аккаунтов и постов
- [ ] Логи содержат полную информацию о всех действиях
- [ ] Система стабильно работает 24/7

---

**Версия:** 1.0  
**Дата:** 03.12.2025  
**Автор:** Rafayel (Digital Twin Platform Team)

